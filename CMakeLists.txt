cmake_minimum_required(VERSION 3.5)

project(shudao
        VERSION 0.1.0
        LANGUAGES CXX)

set()

option()

add_library()

add_executable()

find_package(mqtt REQUIRED)
target_link_libraries( PRIVATE ${})
